// TODO mason make templates with placeholder vars
// take output and place in a project with build_runner dev dep and other codegen stuff setup
// run code gen then take generated files replace placeholder vars with template vars and move the
// the result into the repsective template.
// This way the generated files are always up to data to latest generator changes
