# Domain Command

Work with the domain part of an existing Rapid project.

```
rapid domain <subcommand>
```

TODO

## Subcommands

<Tabs
  values={[
    { label: "add", value: "add" },
    { label: "remove", value: "remove" },
    { label: "<subdomain>", value: "subdomain" },
  ]}
>
<TabItem value="add">
Add subdomains to the domain part of an existing Rapid project.

```
rapid domain add <subcommand>
```

## Subcommands

Add subdomains of the domain part of an existing Rapid project.

```
rapid domain add sub_domain <name>
```

TODO

</TabItem>
<TabItem value="remove">
Remove subdomains from the domain part of an existing Rapid project.

```
rapid domain remove <subcommand>
```

## Subcommands

Remove subdomains of the domain part of an existing Rapid project.

```
rapid domain remove sub_domain <name>
```

TODO

</TabItem>
<TabItem value="subdomain">
Work with the subdomain `<subdomain>`.

```
rapid domain <subdomain> <subcommand>
```

## Subcommands

<Tabs
  values={[
    { label: "add", value: "add-1" },
    { label: "remove", value: "remove-1" },
  ]}
>
<TabItem value="add-1">
Add a component to the subdomain `<subdomain>`.

```bash
rapid domain <subdomain> add
```
</TabItem>
<TabItem value="remove-1">
Remove a component to the subdomain `<subdomain>`.

```bash
rapid domain <subdomain> remove
```
</TabItem>
</Tabs>

</TabItem>
</Tabs>
