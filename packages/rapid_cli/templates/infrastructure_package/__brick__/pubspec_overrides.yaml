# melos_managed_dependency_overrides: {{project_name}}_domain,{{project_name}}_logging,rapid_domain
dependency_overrides:
  rapid_domain:
    path: ../../../../../../../rapid/rapid_domain
  {{project_name}}_domain:
    path: ../../{{project_name}}_domain/{{project_name}}_domain
  {{project_name}}_logging:
    path: ../../{{project_name}}_logging
