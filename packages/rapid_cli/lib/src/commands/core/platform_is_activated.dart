import 'package:rapid_cli/src/core/platform.dart';

/// Signature for method that returns whether [platform] is enabled in a Rapid project.
typedef PlatformIsActivated = bool Function(Platform platform);
