// coverage:ignore-file

import 'project_windows_windows_app_localizations.dart';

/// The translations for English (`en`).
class ProjectWindowsWindowsAppLocalizationsEn
    extends ProjectWindowsWindowsAppLocalizations {
  ProjectWindowsWindowsAppLocalizationsEn([String locale = 'en'])
      : super(locale);
}
