// import 'package:test/test.dart';
// import 'package:{{project_name}}_domain{{#has_sub_domain_name}}_{{sub_domain_name}}{{/has_sub_domain_name}}/src{{^output_dir_is_cwd}}/{{{output_dir}}}{{/output_dir_is_cwd}}/{{name.snakeCase()}}.dart';

void main() {
/*   group('{{name.pascalCase()}}', () {
    group('.()', () {
      test(/* TODO: valid */, () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}(/* TODO: valid */);

        // Assert
        expect(
          {{name.camelCase()}},
          isA<Valid{{name.pascalCase()}}>().having(
            (e) => e.value,
            'value',
            /* TODO: valid */,
          ),
        );
      });

      // TODO: more valid cases (optional)

      test(/* TODO: invalid */, () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}(/* TODO: invalid */);

        // Assert
        expect({{name.camelCase()}}, isA<{{name.pascalCase()}}FailureFoo>());
      });

      // TODO: more invalid cases (optional)
    });

    group('.random()', () {
      test('creates valid instance', () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}.random(valid: true);

        // Assert
        expect({{name.camelCase()}}, isA<Valid{{name.pascalCase()}}>());
      });

      test('creates invalid instance', () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}.random(valid: false);

        // Assert
        expect({{name.camelCase()}}, isA<{{name.pascalCase()}}FailureFoo>());
      });
    });

    group('getOrCrash', () {
      test('returns value when valid', () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}(/* TODO: valid */);

        // Assert
        expect({{name.camelCase()}}.getOrCrash(), /* TODO: valid */);
      });

      test('throws StateError when invalid', () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}(/* TODO: invalid */);

        // Assert
        expect({{name.camelCase()}}.getOrCrash(), throwsStateError);
      });
    });

    group('isValid', () {
      test('returns true when valid', () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}(/* TODO: valid */);

        // Assert
        expect({{name.camelCase()}}.isValid(), true);
      });

      test('returns false when invalid', () {
        // Arrange + Act
        final {{name.camelCase()}} = {{name.pascalCase()}}(/* TODO: invalid */);

        // Assert
        expect({{name.camelCase()}}.isValid(), false);
      });
    });
  }); */
}
