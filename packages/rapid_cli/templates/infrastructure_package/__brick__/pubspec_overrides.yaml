# melos_managed_dependency_overrides: {{project_name}}_domain,{{project_name}}_logging
dependency_overrides:
  {{project_name}}_domain:
    path: ../../{{project_name}}_domain/{{project_name}}_domain
  {{project_name}}_logging:
    path: ../../{{project_name}}_logging
